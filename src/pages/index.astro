---
const acceptLanguage = Astro.request.headers.get("Accept-Language");

if (acceptLanguage && /^pt-br/i.test(acceptLanguage)) {
  return Astro.redirect("/pt-br/");
}

return Astro.redirect("/en/");
---
